import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: '<app-starlink-visualizer></app-starlink-visualizer>',
  styles: [':host { display: block; height: 100vh; margin: 0; }']
})
export class AppComponent {}
